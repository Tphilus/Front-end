export function sayHello(name: string): void {
  console.log(`Hello ${name}`);
}

export let person = "susan";

type Student = {
  name: string;
  aga: number;
};

const newStudent = {
  name: "person",
  age: 24,
};
